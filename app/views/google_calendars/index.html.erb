<% if current_user.google_authentication.present? %>
  <%= link_to "#{create_stop_channel_link_text} channel", create_or_stop_channel_path %>
  <%= link_to "Revoke Google calendar access", revoke_calendar_access_path %>
<% else %>
  <%= link_to "Sync Google Calendar", redirect_to_google_calendar_auth_path %>
<% end %>
<%= link_to "Sign Out", destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?'} %>